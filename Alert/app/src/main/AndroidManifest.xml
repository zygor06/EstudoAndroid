<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alert.hygor.com.br.alert">

    <application
        android:allowBackup="true"
        android:icon="@drawable/h_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CheckBoxActivity" />
        <activity android:name=".AlertActivity" />
        <activity android:name=".SeekBarActivity" />
        <activity android:name=".SerieActivity" />
        <activity android:name=".RadioButtonActivity" />
        <activity android:name=".ToggleActivity" />
        <activity android:name=".MidiasActivity" />
        <activity android:name=".SharedPreferencesActivity" />
        <activity android:name=".CorUsuarioActivity" />
        <activity android:name=".AnotacoesActivity" />
        <activity android:name=".SQLiteActivity" />
        <activity android:name=".ListaTarefasActivity" />
        <activity android:name=".FragmentsActivity"></activity>
    </application>

</manifest>